# PostgreSQL local para desenvolvimento
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: rp_owner
      POSTGRES_PASSWORD: rp_owner
      POSTGRES_DB: residencia_planner
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
